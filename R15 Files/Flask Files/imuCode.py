import random
import math

def angle():
    #pitch = -random.random() * 360
    #yaw = random.random() * 360
    #roll = random.random() * 360

    pitch = 90
    yaw = 0
    roll = 0
    angle = [pitch, yaw, roll]
    
    return angle